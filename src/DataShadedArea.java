
public class DataShadedArea extends ShadedArea{
    public String Data;

    DataShadedArea(String sectionTitle, Rectangle rect, int rowNo, String[] label, LabelAxis labelAxis){
        super(sectionTitle, rect, rowNo, label, labelAxis);
    }
}